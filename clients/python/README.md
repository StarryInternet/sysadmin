## Install

### PyPI

```sh
pip install py-sysadmin
```

### Development

```sh
pip install -e .[test]
pre-commit run -a
nosetests
```

Note that sysadmin needs to be built in order to run some of the tests.
